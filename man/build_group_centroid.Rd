% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_group_centroid.R
\name{build_group_centroid}
\alias{build_group_centroid}
\title{Build Group-Level LSA Centroid}
\usage{
build_group_centroid(
  df,
  corpus = NULL,
  participant_num = "participant_num",
  text = "text",
  time = "X",
  k = 100,
  verbose = TRUE
)
}
\arguments{
\item{df}{A data frame containing at minimum a participant identifier, text column, and time column.}

\item{corpus}{An optional prebuilt `tm::Corpus` object. If `NULL`, a corpus is generated from the `text` column.}

\item{participant_num}{The name of the column identifying participants. Defaults to `"participant_num"`.}

\item{text}{The name of the column containing text data. Defaults to `"text"`.}

\item{time}{The name of the column containing time or ordering information. Defaults to `"X"`.}

\item{k}{The number of dimensions to retain in the LSA-reduced space. Defaults to 100.}

\item{verbose}{Logical. Should progress messages be printed? Defaults to `TRUE`.}
}
\value{
A list of class `"build_group_centroid_result"` containing:
\describe{
  \item{lsa_result}{The result of `lsa::lsa()` containing the reduced-space representation.}
  \item{centroid}{A vector representing the average position of all documents in LSA space.}
  \item{corpus}{The `tm::Corpus` object used.}
  \item{dtm}{The term-document matrix.}
  \item{df}{The input data frame, relabeled to standard internal columns.}
}
}
\description{
This function computes a Latent Semantic Analysis (LSA)-based centroid representing
the semantic content of a group conversation. It performs dimensionality reduction
on a term-document matrix constructed from a provided data frame and returns a
structured object containing the centroid, LSA results, and other relevant metadata.
}
\examples{
\dontrun{
result <- build_group_centroid(my_data, k = 100)
str(result$centroid)
}
}
